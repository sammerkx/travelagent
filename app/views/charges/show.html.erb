<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @charge.name %>
</p>

<p>
  <strong>Amount payable:</strong>
  <%= @charge.amount_payable %>
</p>

<p>
  <strong>No of persons:</strong>
  <%= @charge.no_of_persons %>
</p>

<%= link_to 'Edit', edit_charge_path(@charge) %> |
<%= link_to 'Back', charges_path %>
